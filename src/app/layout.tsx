import type { Metadata } from "next";
import type { ReactNode } from "react";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "SLATE – Scalable Learning & Adaptive Text Engine",
  description: "Transform your lecture recordings into intelligent, structured study notes powered by AI.",
  keywords: "AI notes, lecture transcription, study tool, SLATE",
};

export default function RootLayout({
  children
}: Readonly<{
  children: ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.className} antialiased`}>{children}</body>
    </html>
  );
}
